spring:
  application:
    name: vscan-api
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
  cloud:
    aws:
      sqs:
        region: eu-central-1
        queue:
          file-upload-queue: ile-upload-queue-test
      s3:
        region: eu-central-1
        dirty-bucket: vscan-api-dirty-storage-test
      dynamodb:
        region: eu-central-1
        file-scan-result-table: FileScanResults-test
  mail:
    port: 587 #change in SSM
    host: smtp.vscan.dataslab.eu #change in SSM
    from: noreply@vscan.dataslab.eu
    to: seg@vscan.dataslab.eu
    subject: "Scanning file. File id: "

management:
  endpoints:
    web:
      exposure:
        include: info, health
      base-path: /
  endpoint:
    health:
      show-details: always

logging:
  level:
    com.dataslab.vscan: debug
    org.springframework: info


